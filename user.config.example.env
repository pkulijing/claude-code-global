# claude-code-global 用户可配置项 —— 示例 / 默认值（committed）
#
# 这是「示例基线」，扮演 .env.example 的角色：只含默认值与说明，可安全提交。
# install.sh 会据此 seed 出你的【真实配置】（默认 ~/.claude-code-global/config.env）：
#   - 真实配置落在仓库外 → git pull / 多设备 auto-update 永不覆盖你的改动；
#   - 已存在的 key 绝不被覆盖（user-wins）；
#   - 本文件未来新增的 key，会在下次 install 时「补缺追加」默认值到你的真实配置，
#     已设值不动。
# 改完真实配置后，下次 `bash install.sh`（或自动同步）即生效。
#
# 语法：每行一项 KEY=value；# 起注释；值含空格可用引号包裹。

# git init 默认分支。install.sh 据此执行 `git config --global init.defaultBranch <值>`，
# 之后任何 `git init`（你手动跑或未来 skill 自动跑）都会用这个分支名。
# 置空（写成 GIT_INIT_DEFAULT_BRANCH=）= 不改动你的 git 全局配置（opt-out）。
GIT_INIT_DEFAULT_BRANCH=master

# —— 后续可配置项将逐步在此追加（如 commit trailer 署名等）——
# —— 机制设计见 docs/27-用户可配置项机制/DESIGN.md ——
