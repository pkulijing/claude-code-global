# Review 迭代留痕

本轮改的是 `/review-loop` 自身。因软链指向主 checkout（worktree 内改 skill 不即刻生效），当前会话**手动按新 SKILL.md 的规则跑**——正好活体验证新的 orchestrator 委派路径（round 48 同款自举法）。

- 选档：diff 全为 `skills/*.md` + `GLOBAL_AGENTS.md` + `README.md` 指令规则文件 → Step 2 判定**不跳过**；不命中复杂特征 → **默认档**（orchestrator `sonnet`，扇出 3 个 `sonnet` reviewer）。
- 闸 A（运行验证）：**N/A**——纯指令 / 文档，无可运行代码单元。

## 委派前自查（未计入迭代轮数）

发起委派时发现新模板里的 `run_in_background: false` 参数已从当前 Agent 工具 schema 移除（照抄会校验失败）——与 round 48 逮到的「模板漏必填 `description`」同源：**Agent 工具 schema 随 CC 版本漂移**。修复：模板降格为「要点示意」，明示按环境实际 schema 填参、拿到 finding 才往下走。orchestrator 审到的是修复后的工作树。

## 第 1 轮

**编队实测**：orchestrator 成功用 Agent 工具并行起 3 个 reviewer（角度①浅层 bug / ②跨文件契约 / ③规范合规），未触发「顺序执行」内部降级——**嵌套扇出在当前环境可行**。

**finding：0 条 ≥80。** 2 条低置信项（均 ~50）分诊为不阻断、不修：

| #   | 位置                      | 问题                                                       | 分诊                                                                                                                      |
| --- | ------------------------- | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| 1   | `GLOBAL_AGENTS.md:51,:86` | 降级链用词「本端」与 SKILL / README 的「主会话」不统一     | **有意为之**——宪法面向 CC / Codex 两端，Codex 无「主会话/子会话」概念，「本端」才准确；SKILL 是 CC 专属文档故说「主会话」 |
| 2   | `GLOBAL_AGENTS.md:80`     | 「本节只留骨架」自我定性 vs 小节仍保留具体参数，不完全相符 | nit，不影响执行；参数重复模式 pre-existing、本次未恶化                                                                    |

orchestrator 另核实：内部引用无断链、计数词与列表项吻合、编队规格四文件一致、无旧机制表述残留、简体中文合规、`/code-review` 残留提及均为有意。

**三要素并闸全过 → review clean ✅**（自动修复 0 轮，未触及 2 轮上限）

## 新委派路径的实测数据

| 项                  | 值                                  |
| ------------------- | ----------------------------------- |
| orchestrator tokens | 117,401（sonnet，含 3 个 reviewer） |
| 工具调用            | 24                                  |
| 耗时                | ~11 min                             |
| 主会话收到          | 一份 finding 列表（含核实要点摘要） |

对比 round 48 旧路径（委派跑 `/code-review medium`：单轮 ~17 万 token）：新路径首轮 ~12 万 token 且不再依赖任何外部命令的可调用性。
