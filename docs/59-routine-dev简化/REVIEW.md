# REVIEW —— round 59 · `/routine-dev` 简化

编队：重档（`review-orchestrator` ×1 + `code-reviewer` ×4 角度 ①②③④ + `code-reviewer-deep` ×1 角度 ⑤），走第 ① 档，未降级。

## 第 1 轮

角度 ①②③④ 全 clean；角度 ⑤（深审）报 2 条过 80 分线。两条同一根因：**刀 C 把「放弃这条」升格成「放弃本次运行」，而刀 A 恰好拿掉了唯一的跨运行记忆**，两刀单独看都没问题，叠起来产生组合效应。

| # | 置信 | 内容 | 怎么修的 |
| --- | --- | --- | --- |
| 1 | 88 | **确定性活锁**：1.3 排序是纯函数、分诊短路只读到中标那条，开发期任一分岔都判「放弃本次运行」，又无跨运行标记 → 队首一条确定性失败会每周三次原地复现，后面的 issue 永久饿死，且零 PR 时无出口 | 开发期分岔改为 **`git restore` + 换下一条候选**，回 1.3 继续往下找，最多尝试 3 条；只有「候选耗尽 / 到上限 / push 或开 PR 失败」才结束本次运行 |
| 2 | 80 | **落点预判失准 + 无标记 → 一个挂着的在途 PR 锁死 routine**：1.2 只按预期落点过滤，而「共享登记文件也算落点」那条通则被连同合批节一起删掉了（它原先寄生在那一节里，是这道预判真正的判据）→ 跑完整轮开发才在真实 diff 复核发现相交 → 下次逐字重复 | ① 把登记文件通则补回 1.2 撞车行下方；② 真实 diff 相交时同样改为「换下一条候选」 |

**明确没有采用的修法**：重新引入跨运行持久标记。它能更彻底地解活锁，但与本轮刚收紧的「绝不打任何 label」直接冲突 —— 用安全边界换吞吐不划算。该选项连同它被否的理由已写进 SKILL 的「残余」块。

丢弃（< 80，不阻断）：`platform_issue.py` 权限位 755→644（55，无直接执行路径）、`refs/pull` fetch 缺 `+` 前缀（68）、`git branch -D` 删当前分支会被拒（72）、push 成功但开 PR 失败留下无主远端分支（60）。

## 第 2 轮（收窄：核对上两条是否消除 + 修复有无引入新问题）

**第 1 轮两条均已消除**（5 个 reviewer 独立核对：分岔契约表内除「push / 开 PR 失败」这条正当的基建故障终止外，无遗漏的「放弃本次运行」残留；登记文件通则位置与措辞可执行）。安全边界逐条复核未被弱化，**未发现任何为解决饿死而偷渡的持久化写**。

新报 1 条过线：

| # | 置信 | 内容 | 怎么修的 |
| --- | --- | --- | --- |
| 3 | 85 | **重试循环清理不干净**：`git restore` 只做「工作树 ← 暂存区」，对已 `git add` 的是 no-op、对未跟踪新文件完全无效，而 `git checkout <默认分支>` 会把这些残留**原样带过去**。于是候选 A 写下的文件混进候选 B 的 PR，描述却只写 `Closes #B`。**撞红线那条分岔尤其致命** —— A 正因写到 `agents/` / `install.sh` / `.github/` 才被放弃，残留被带进 B，而开 PR 前的真实 diff 复核只比对在途 PR 落点、不重新判红线，红线被静默绕过 | 把清理写成**规范定义**：`git reset --hard` → `git clean -fd` → `git checkout <默认分支>` → `git branch -D` → 复检 `git status --porcelain` 为空；契约表各行改为指向它。并写明敢用 `git clean -fd` 的前提是 Step 0 前置闸已要求工作树干净 |

同轮另修一处跨文件编号错位（置信 90）：`security-boundary.md:10` / `:58` 仍写「Step 3 把它写进文件 / Step 4 第 5 段那张表」，按重编号后应为 Step 2 / Step 3。

## 未收敛遗留

**Finding 3 的修复未经复审。** `/review-loop` 的 2 轮自动上限已用满（第 1 轮修 → 第 2 轮修），按硬规则停环留痕放行，不再起第 3 轮。

- **风险面**：那处修复是本轮最后写下的规范清理块，改的是安全边界文件里一段新机制，**没有独立 context 看过**。
- **已自查的部分**：`check-refs` 零失效引用、`platform_issue.py --self-test` 全绿、契约表各行与规范定义的指向一致（人工逐行核过）。
- **建议**：`/finish` 前人工再看一眼那一段（`skills/routine-dev/SKILL.md`「无人值守分岔契约」里的清理步骤块），或单起一轮 review 复核。

## 机械闸

| 闸 | 结果 |
| --- | --- |
| A · 运行验证 | 指令规则文件无可运行单元，判 **N/A**；配置面由 `platform_issue.py --self-test`（OK）与人工核 YAML 承担 |
| `check-refs` | 无失效引用 ✅（改动前后各跑一次） |
| B · 无高置信 correctness finding | 第 1 轮 2 条已修，第 2 轮 1 条已修但**未复审**（见上） |
| C · 已定前提未被重复质疑 | 两轮均无 finding 落在已定前提上（10 条前提逐轮转传编队） |

## 收尾 commit（SUMMARY / DEVTREE / README）的第 1 轮

未走 Step 2 自动跳过：SUMMARY 与 DEVTREE 属 `docs/`，但 README 新增的是**流程说明段**、且复述的是一条安全边界（写权限面穷举三样），复述失真比不写更糟，按「有疑则不跳」起默认档编队。

角度 ①②③ 均 clean（DEVTREE 三处互相一致、mermaid `~~~` 成对、README 措辞与 `security-boundary.md` 逐义对得上、SUMMARY 的绝对数字经独立探针核实准确）。orchestrator 自己的探针另报 1 条过线：

| # | 置信 | 内容 | 怎么修的 |
| --- | --- | --- | --- |
| 4 | 95 | `DEVTREE.md:284` 写「routine-dev 不再是最大单文件」，**与实测相反**：`measure` 显示 `skills/routine-dev/SKILL.md`（15,999）仍排第一，高于 `playbooks/python.md`（14,750）与宪法（11,107）。三个 reviewer 都核过「19,356 → 15,999」这组绝对数字（准确），**没人核由它派生出的比较性结论** | 改为如实陈述「仍是最大单文件，只是对宪法的差距从 8,249 收窄到 4,892」，DEVTREE 与 SUMMARY 各写一处 |

> **这条值得记**：绝对数字对、由它推出的比较结论错，是很难被察觉的一类失真 —— 每个数字单独查都能对上，读者却会据结论形成相反的心理模型（「下一个精简目标不是它了」）。**核数字不等于核结论。**

## 收尾 commit 的第 2 轮

Finding 4 已消除（orchestrator 实跑 `measure` 复核：15,999 仍排第一，8,249 / 4,892 两个差值算术成立，DEVTREE 与 SUMMARY 两处表述一致）。三名 reviewer 角度 ①②③ **全部 clean**。

另按要求把所有百分比逐个验算过：−17%（实算 −17.34%）、−39%（−38.78%）、−2.8%（−2.818%）、局限性里的 −18%（字节口径 38,292 → 31,206，实算 −18.5%，与 PROMPT 的十进制 KB 口径一致，未混用字符与字节）。

**三要素并闸全过 → review clean ✅**
