# 首次 dry-run 记录（本机，`/routine-docs --dry-run` 的等价手跑）

跑的时间点：本轮开发期，仓库 open issue 27 条、open PR 0 条。
目的：**在把 routine 挂上 cron 之前，先验证分诊与合批规则产出的结果是不是人能认可的**——
剧本写得再漂亮，选错 issue 或分批不合理都会每天制造垃圾 PR。

## Step 1.1 硬过滤（按 label / 状态）

| issue                                | 排除理由       |
| ------------------------------------ | -------------- |
| #60、#39                             | `priority:P0`  |
| #7                                   | `area:install` |
| （在途 PR 集合为空，本次无此项排除） |                |

## Step 1.2 模型分诊（读正文判「预期落点是否只在文档」）

**收下 15 条**：

| issue | 预期落点                        | 说明                       |
| ----- | ------------------------------- | -------------------------- |
| #73   | `GLOBAL_AGENTS.md`              | 云端 / 本机 issue 交互分野 |
| #72   | 新建 `rules/cloud-routine.md`   | 云端 routine 能力矩阵      |
| #69   | `rules/lark.md`                 | 最小 scope 授权工作流      |
| #68   | 新建 `rules/scheduled-agent.md` | 双平台定时唤起无头 agent   |
| #67   | `rules/python.md` §5            | CLI 装进隔离目录           |
| #65   | `rules/python.md` §4            | 测试环境变量清场           |
| #64   | `GLOBAL_AGENTS.md`              | 只有人知道的参数必须问人   |
| #63   | `rules/shell.md`                | 长命令写脚本给人跑         |
| #62   | `rules/python.md`               | rich escape                |
| #61   | `GLOBAL_AGENTS.md`              | wrapper 原则               |
| #57   | `rules/ros2.md`                 | launch 可选参数覆盖        |
| #56   | `rules/lark.md`                 | mermaid 围栏转画板         |
| #55   | `GLOBAL_AGENTS.md`              | 关键假设被证伪须停机       |
| #54   | `rules/python.md`               | LLM schema 与 prompt 漂移  |
| #50   | 新建飞书 bot 后端规则           | 长连接幂等 + 卡片回调      |

**排除 9 条**：

| issue    | 排除理由                                                              |
| -------- | --------------------------------------------------------------------- |
| #70      | 解法可能要动 `install.sh` 的软链策略，且方案有分歧——该走 `/start`     |
| #66      | 落点是 `templates/`，可执行面                                         |
| #59      | 落点是 `skills/start/SKILL.md`，门禁自身                              |
| #58      | 实质是改 TDD 纪律 + `/review-loop`，有分歧且触及 skill                |
| #53、#52 | 新增 / 修改 skill                                                     |
| #27、#18 | 探索性、无定论                                                        |
| #34      | **仓库现状已满足**——`rules/frontend.md` §1 已写了这条，疑似可直接关闭 |

## Step 2 合批方案（单次上限 3 个 PR）

| PR       | issue                           | 落点                | 合批理由                        |
| -------- | ------------------------------- | ------------------- | ------------------------------- |
| A        | #67 #65 #62 #54                 | `rules/python.md`   | 同落点文件，review 时上下文连贯 |
| B        | #64 #55 #61 #73                 | `GLOBAL_AGENTS.md`  | 同落点文件，都是流程纪律        |
| C        | #72                             | 新建整份 rules 文档 | 规则 4：新建整份文档独占一个 PR |
| 留到下次 | #69 #56 / #68 / #50 / #57 / #63 | —                   | 超出单次 3 个 PR 上限，自然顺延 |

## 本次 dry-run 的产出（已回写进 skill）

跑之前没想到、跑出来才发现的两条，已补进 `skills/routine-docs/SKILL.md`：

1. **「仓库现状已经满足」要成为一条独立的排除理由**（#34 就是活例子）。不查一眼就动手，会把已经写进
   `rules/frontend.md` 的内容再写一遍，产出一个纯噪音 PR。
2. **落点歧义要有默认**：不少 issue 写「落宪法**或**新增 `rules/<topic>.md`」（#61 #50 #63 都是）。
   默认补进现有文档——新增一份领域规则文档要定触发条件、要同步加宪法指针、会影响所有项目的加载面，
   是个更大的决定，不该由无人值守的 routine 顺手做掉。
