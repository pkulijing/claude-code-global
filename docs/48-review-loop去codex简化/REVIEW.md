# Review 迭代留痕

本轮改的是 `/review-loop` 自身。因软链指向主 checkout（worktree 内改 skill 不即刻生效），当前会话的 `/review-loop` 仍是旧版，故**手动按新 SKILL.md 的规则跑**——正好活体验证新的委派路径。

- 选档：diff 含 `skills/*.md` + `GLOBAL_AGENTS.md` → Step 2 判定**不跳过**（指令规则文件）；不命中任何复杂特征 → **默认档**（委派 `model: sonnet` 子 agent 跑 `/code-review medium`）。
- 闸 A（运行验证）：**N/A** —— 纯指令 / 文档，无可运行的代码单元。

## 第 1 轮

**reviewer 报 7 条**（6 CONFIRMED + 1 PLAUSIBLE）。分诊后 **5 条阻断、2 条不阻断**。

| #   | 位置                                   | 问题                                                                    | 分诊       | 处理                                                                                                                                                                                      |
| --- | -------------------------------------- | ----------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `skills/review-loop/SKILL.md` Step 4   | Agent 委派模板漏了必填字段 `description`，照抄会 `InputValidationError` | 高置信 bug | 补 `description` + 加一条要点说明                                                                                                                                                         |
| 2   | 同上 Step 3 硬规则二 vs Step 5         | 「永远委派、主会话不直接跑」与降级链「委派失败→主会话直跑」字面矛盾     | 高置信 bug | 硬规则二加括号例外，明确指向 Step 5                                                                                                                                                       |
| 3   | 同上 Step 4                            | 删掉旧 Step 0 后，首轮「已定设计前提清单」的来源无人交代                | 高置信 bug | 补一段「怎么来」：本轮对话 + PLAN.md 决策段 + PROMPT.md 已决段                                                                                                                            |
| 4   | `README.md`                            | 「对齐官方 ≥80 阈值」成孤儿引用（数字只存在于本次被删的旧文本里）       | 高置信 bug | 删该措辞                                                                                                                                                                                  |
| 5   | `skills/review-loop/SKILL.md` 明确不做 | 「维护面外溢四个文件」语法不通                                          | 机械       | 改为「维护面外溢**到**四个文件」                                                                                                                                                          |
| 6   | 同上（安全）                           | 「禁读 `.env*` 等敏感文件」的指令随 codex 链路一并删除                  | **不阻断** | pre-existing —— 该指令只约束过 codex 那条外部进程链路，默认的 CC `/code-review` 档从来没有它；委派子 agent 与主会话**同信任边界**。补一条「明确不做」把边界写清楚，免得下个读者以为是漏删 |
| 7   | `GLOBAL_AGENTS.md:51`                  | 降级链摘要只写两级、省略「主会话直跑」                                  | **不阻断** | PLAUSIBLE、低置信；是「极简摘要 + 指向详述」的一贯写法                                                                                                                                    |

修复分流：全部命中 6.2 的「改的就是指令 / 文档本身 → 无红测试可写，直接改」。

## 第 2 轮（复审）

**无 finding。** 5 处修复逐条确认消除，未引入新问题。reviewer 自行 REFUTE 掉一条候选（「移除 codex 后缺少子 agent 输出的噪音过滤指导」——旧噪音过滤针对的是非 agentic 的 codex CLI 原始 stdout，委派的子 agent 是可指令的 LLM，不适用）。

**三要素并闸全过 → review clean ✅**（自动修复 1 轮，未触及 2 轮人工闸口）

## 顺带：新委派路径的实测数据

| 轮次    | 子 agent tokens | 工具调用 | 主会话收到        |
| ------- | --------------- | -------- | ----------------- |
| 第 1 轮 | 173,120         | 54       | 一份 finding 列表 |
| 第 2 轮 | 145,694         | 19       | 一份 finding 列表 |

两轮 review 在子 agent 里烧掉 ~32 万 token（Sonnet 单价），主会话 context 只增加了两份 finding 列表。若按旧规则在主会话裸调 `/code-review`（继承 xhigh、10 个 inline angle + sweep），这 32 万 token 的文件阅读会**全部沉进主对话历史并逐轮重发**——这正是本轮要治的病。
