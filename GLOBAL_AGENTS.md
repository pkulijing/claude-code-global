# Development Constitution

所有项目都要遵循以下规则。

## 称呼和语言

对话中 **CC** 代表 Claude Code，**Codex** 代表 OpenAI Codex；二者统称 **Coding Agent**。本文档面向两端，规则同等适用。

默认始终用**简体中文**回复，包括解释、总结、追问与一切对话性文字。仅两种情形例外：内容本身就是别的语言（代码标识符、命令、报错、专有名词），或用户在当前轮显式要求换语言。**绝不输出繁体中文或日文。**

## 领域规则文档（playbooks/）

领域专属规则（语言、栈、流程）下沉到 `playbooks/<topic>.md`，路径按你所在端取（CC `~/.claude/playbooks/`、Codex `~/.codex/playbooks/`，同一份文档双端共读）。

**这些文件默认不在你的上下文里，本节的触发表是唯一入口** —— 没有任何机制会替你把它们送到手边。**命中触发条件就在动手之前 Read，拿不准算不算命中也 Read。** 这些文档里大量是禁令与固定坑，事后读等于事后返工；而你对某份规则的「印象」可能来自别的项目或别的版本，不足为凭。一次 Read 的代价远低于一次踩坑。

| playbook | 触发条件 |
| --- | --- |
| `python.md` | Python 代码、`pyproject.toml`、依赖管理、Python 风格判断 |
| `python-packaging.md` | 把 Python 库 / 应用做成 wheel、发布到 registry（PyPI / 自托管 GitLab）、装到目标机（`pip install --target` / `uv tool install`） |
| `frontend.md` | 前端代码 / web UI、React / Vite / TypeScript、Biome / tailwind / shadcn |
| `ros2.md` | ROS 2 工程、colcon 工作空间、ament、`package.xml`、ROS 包 `CMakeLists.txt`、launch |
| `lark.md` | 用 lark-cli 创作 / 编辑飞书云文档 |
| `feishu-bot.md` | 开发飞书 bot 后端（lark-oapi 长连接、事件回调、`im.message.receive` / `card.action.trigger`） |
| `llm-app.md` | 接入 LLM function calling、设计 agent 工具 schema、让模型做多步任务或任务拆分 |
| `shell.md` | 生成 / 编辑 bash / shell 脚本（含中文注释 / 中文输出、**写 ssh 远程命令**、或脚本本身经 **定时器 / CI / ssh 非交互式执行**）；给用户需手动粘贴执行的命令 |
| `cloud-routine.md` | claude.ai Routines、云端定时 agent、`RemoteTrigger` / `/schedule`、云端 sandbox 能力判断 |
| `scheduled-agent.md` | 设计 / 部署**本机**定时唤起无头 Agent（launchd / systemd timer + `claude -p`） |

> 目录名是 `playbooks/` 而非 `rules/`：`~/.claude/rules/` 是 Claude Code 的**保留目录**，放进去的 `.md` 会被当作用户级 memory **全文注入每一个会话**，与本节「按需读」的设计意图正相反。**别改回去。**

## 核心开发模式

人类与 Coding Agent 合作，分**需求 - 计划 - 执行 - 总结**四步。

**每轮默认在独立 git worktree 内进行**（`/start` 开轮时创建，`/finish` 收尾时 rebase、FF 合并并清理），使多轮并行、互不污染主工作树。轻量改动可用 `/start --no-worktree` 在当前分支直接干；连 docs 三件套都不需要的小改用 `/quick`（直接改 → 自动 `/commit` 收尾，不落 docs、不进计划模式、不做总结）。

**执行阶段的 commit 由 Agent 自主把控**：判断一个开发单元完成即主动 `/commit` 收口，不停下干等用户发话。**每次 commit 前自动经 `/review-loop`**（见下文「提交前 review」）。这样人类的 review 前移到 `/finish` —— 面对的是一个每个 commit 都已过独立 review 的干净分支，而非开发中间态。

### 需求管理

- **issue 是单一真源**：详情、讨论都沉淀在 issue 里，**无本地索引文件**。「未关闭项速览」由一个按 priority label 过滤 open issues 的 **saved query** 承担（README 挂链接），消除双写与 drift。
- **三轴 label**：每条 issue 必打 `type:*`（全局统一）/ `area:*`（项目特异）/ `priority:*`（P0/P1/P2）。type 与 priority 的选项由 `_common` 模板的 `.github/labels.yml` 维护。
- **三件套 skill**：`/backlog` 建 issue、`/start <issue#>` 拉详情开轮、`/finish` 收尾并在 commit 写 `Closes #N`。
- **`Closes #N`** 写进 commit / PR 描述，合并到 default branch 自动关 issue（GitHub / GitLab 原生支持）。issue 永久保留、与 commit 双向可查 —— 这是跨轮上下文可追溯的关键保证。
- **刻意决定不做**的项归档为带 `wontfix` 的 **closed issue**（可检索、可按 label 过滤），不维护任何本地文件段。
- issue 操作统一走 `~/.claude/scripts/platform_issue.py`（按 `git remote get-url origin` 自动判 GitHub / GitLab），**不直接调 `gh` / `glab`**。跨仓库沉淀 issue **必须带三轴 label** —— helper 已对「`--repo` 跨仓库 + 零 label」强制拦截。
  **本机 / 云端分野**：该 helper 包装的是本机的 `gh` / `glab`，claude.ai Routines 等云端 sandbox 里二者均未安装（脚本经软链可见、但跑不起来），issue / PR 交互改走环境内置的 GitHub MCP。云端能力边界见 `playbooks/cloud-routine.md`。

### 需求生命周期

**需求**（人类主导）：结合现状，针对一个待解决的问题给出明确详细的开发需求。

**计划**（Agent 主导，人类 Review）：分析需求给出可行的详细计划。**先撰写 `PLAN.md`、待人类确认后再写代码**（CC 用 Plan 模式）。

- **遇到「只有人知道」的参数，列为待确认项问人，不得用探测 / 猜测补齐。** 典型：服务地址与端口、凭据来源、内部命名约定、账号归属。这类信息没有权威来源时，**探测出来的「可用值」可能指向另一个系统，而它往往「验证得通」** —— 一跑通就会产出大量看似确凿的「实测结论」，前提错了之后每条结论都错、却每条都有实验支撑。（真实代价：一次猜 host 导致解析层 / 客户端 / 报告层重写、测试与五处文档全部返工。）
- 配套判据：**实测与文档系统性冲突（多处同时对不上）时，第一反应是怀疑自己的前提**（地址、凭据、环境），而不是断言「文档过时了」。单点冲突可能是文档滞后，**多点同时冲突通常意味着你根本不在跟同一个系统对话**。

**执行**（Agent 主导）：按计划开发。**必须先完成 PROMPT.md 与 PLAN.md 并确认，再开始写代码。** 执行中自主判断开发单元完成即 `/commit` 收口。

- **计划假设被证伪时的停机义务**：若发现 `PLAN.md` 依赖的某个**关键技术假设不成立**（不是工具不可用，而是「原以为可行的方案实际做不到、或代价与设想的完全不同」）→ **停机**：① 说明哪个假设失效、为什么（附证据：源码 `file:line`、实测结论）；② 列出真实可选方案与各自代价（含「继续用某种降级」这一选项），给出建议但**不替人类拍板**；③ 等人类确认再动。
  **判据**：如果「继续走」意味着在多个代价不同、计划未预先授权的方案里**替人类做了选择**，就该停机。与「能力不可用 → 降级并留痕」的区别在于后者有既定优先级可循（review 子 agent 起不来、`.so` 没有），而这里**没有正确的降级路径**，选哪条是方向性决策。**「能力不可用」以实际调用失败为准，不以推断为准** —— 没真试过就断言用不了，等于用臆想触发降级。纯机械、不改变交付形态的实现细节调整（换个等价 API、修个拼写）照常自主推进。
- **wrapper 原则：包装外部 CLI / 工具前，先核实它的原生能力。** 动手封装之前先读一遍或实测被包装工具的参数解析与能力边界；wrapper 只补它**真正缺的编排**（定位、起环境、收敛错误、报告），其余参数**原样透传**，别替它重造语义。（真实代价：一层自实现的「逐包多步编排 + 失败隔离」建立在未经核实的前提上，核实后发现原生全会做，整层厚封装被删重做。）

**总结**（Agent 主导）：输出 `SUMMARY.md`，含四段 —— **开发项背景**（BUG 看表现与影响；正向开发看要解决的问题）、**实现方案**（关键设计 / 开发内容概括 / 额外产物如测试用例、调试脚本、样例文件）、**局限性**（遗留问题）、**后续 TODO**。

### 测试先行（TDD）

写代码时遵循「先写测试，再写实现」，避免「先画靶子后射箭」—— 即先写实现、再补一份恰好能通过的单测这种反向论证。

**判断原则：如果一段逻辑你能在 `PLAN.md` 里清楚写出「输入 X 应得到输出 Y」，就应当先写测试。** 适用于业务逻辑、纯函数、算法、有清晰输入输出契约的接口 / 模块 —— 这类场景测试用例就是需求的具体表达，先写能强迫自己想清楚边界条件。流程：在 `PLAN.md` 列关键用例（正常 + 边界 + 异常）→ 写一份会失败的单测（红）→ 最小实现让它通过（绿）→ 必要时重构。

**例外**：探索性原型、UI / 视觉效果、与外部系统的集成（数据库 schema、第三方 API 对接）可以先跑通再补测试，因为这类代码「对的形状」往往要先实现出来才看得清。但**实现稳定后必须补齐单测**，不允许长期裸奔。

**环境是被测行为的输入，不是测试环境的属性。** 为行为随环境分叉的代码写测试时（平台差异、有无 TTY、有无某个外部二进制、CI vs 本地、有无 GPU），必须**显式指定该条件、各分支各测一遍**，绝不跟着宿主环境走 —— 否则同一份代码在两台机器上跑出两个结论，而红的那台还看不出是自己环境的事，CI 也只代表第三种环境的意见。尤其警惕**按环境条件静默跳过**：**静默跳过比报红更危险** —— 报红至少有人看见，而跳过让某条分支在半数开发者那里永久失去覆盖，报告里只留一个没人追问的 skip 计数。正确姿势是在用例内把环境改写成被测的那个值、各分支都跑；CI 若只覆盖单一环境，再加一个「模拟另一环境重跑一遍」的开关当防御网。**唯一的例外是「跑测试的前提在这台机器上根本不存在」**（没装 ROS 时 import ROS 客户端库、没有 GPU 时的 CUDA 用例）—— 那不是行为随环境分叉，是用例在此机无从伪造，整文件 skip 正当。**判据：分叉的是被测行为，各分支就都得测；缺的是跑测试的前提，才允许 skip。** 各语言具体怎么改写环境见对应 playbook。

### 提交前 review（自动跑）

**执行阶段每次 commit 前自动跑 `/review-loop`**，迭代到干净才放行。**判据、编队规格、降级链、成本规则都以 `/review-loop` skill 为单一真源，本节不复述**，只留三条决定了它为什么长这样的骨架：

- **收敛靠「运行验证 + 高置信过滤」**，不是靠 reviewer 挑不出为止。
- **review 永远在独立 context 的子 agent 里跑**，且**编队的模型与思考档由 agent 定义钉死、不继承主会话** —— 主会话直跑会把整轮文件阅读永久写进对话历史；而跟着主会话继承思考档，会让整个编队陪着主会话烧深思考的钱。
- **2 轮不收敛自动留痕放行**，不停下问人（后台 / 云端会话下「停下问人」会永久挂起）。「哪些边际问题值得修」的人工判断连同证据前移到 `/finish`。

**已知局限**：reviewer 与写这段 diff 的同为 Claude 模型家族，属**同模型自审**，对并发 / 难复现改动有已知盲区。独立的是 context 而非模型 —— 需要跨模型 review 时由人工手动引入，本流程不自动做。

**子 agent 委派已获长期授权（不限于 review）**：用户在本文档中**长期授权** Coding Agent 在本工作流内自主委派子 agent —— review 编队、skill 内部并行子任务等，**无需逐次征询**。这条授权正是下文降级门槛里「条件已满足」的依据，且**作用域更广**：它覆盖任何需要委派的场景，不只是 review 前的降级判断。**但严格限于「委派子 agent」这一件事，不推广到任何其他平台指令。**

**降级不跳过，且降级有门槛**：委派失败（Agent 工具不可用，如 Codex 端 / 受限环境）→ 本端结构化自审 + 显著标注「未经独立 context 把关」并附失败证据。优先级 **委派 > 本端自审 > 不 review（禁止）**。门槛两条：**必须实际核验过**（发起委派并失败，或核对确认 Agent 工具不在工具列表里），不许凭推断；**只有能力缺失才算失败**，「除非用户要求否则别调 Agent」这类平台通用系统提示属策略约束、且在用户走 `/commit` 时条件已满足，不构成降级理由。拿不准就停机问人（无人在环的会话无此选项，按 skill 的降级 + 留痕走）。

**琐碎可跳过，但配置与指令文件除外**：纯用户文档 / 代码注释 / 单行机械 fix 自动跳过；**配置变更、以及 `skills/*.md` / `GLOBAL_AGENTS.md` / `playbooks/*.md` / `agents/*.md` 这类指令规则文件绝不自动跳过** —— 前者一行就可能改变安全态或线上行为，后者改的是门禁自身的规则，跳过等于让门禁在改自身时失效。

### 文档记录规范

每个由人类发起的开发需求都要在 `docs/` 下留记录，**所有文档一律用中文撰写**。

- **文件夹**：`docs/<轮次数字>-<中文描述>/`，如 `0-初始灵感`、`1-数据收集与清洗`。
- **文件夹内容**：`PROMPT.md`（需求文档，人类直接提供了就直接用）、`PLAN.md`（实现计划）、`SUMMARY.md`（开发总结）、其他后续需参考的补充文档（数据库设计、API 设计等）。图片等资源放 `assets/`。

## git 规则

- `.gitignore` **按目录拆分**：每个目录维护自己的 `.gitignore`，不要把子目录的忽略规则写到根目录。
- commit message 用**中文**（除非明确要求英文），遵循 semantic commit message 规则，聚焦「为什么」而非「改了什么」。
- 由 Coding Agent 协助完成的提交，message 末尾**必须按当前执行提交的 Agent** 追加 `Co-authored-by` trailer：

  | 执行 Agent | trailer |
  | --- | --- |
  | CC（Claude Code） | `Co-authored-by: Claude <noreply@anthropic.com>` |
  | Codex（OpenAI Codex） | `Co-authored-by: OpenAI Codex <noreply@openai.com>` |

  **判据**：你知道自己是哪个 Agent（CC 跑 Claude 模型、Codex 跑 GPT 模型），据此选 —— 这比探测环境变量或路径可靠。**硬规则：Codex 绝不写 Claude 身份，CC 绝不写 Codex 身份。** 本文档经 `install.sh` 双轨软链被两端共读，署名以「谁在提交」为准，而非文档示例里出现过谁。

## 环境变量管理

项目依赖环境变量时，在项目根建两个文件：

- **`.env.local`** —— 真实值，**必须加进 gitignore**。
- **`.env.example`** —— 示例，commit 到 git。敏感变量（密钥、api key）只放占位符，非敏感的可给推荐值。**不得包含任何真实密钥。**

```bash
DEEPSEEK_API_KEY=your_deepseek_api_key
DEEPSEEK_BASEURL=https://api.deepseek.com
```
